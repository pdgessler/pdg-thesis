\usepackage{tikz}     % draw stuff
\usepackage{pgfplots} % plot stuff
\pgfplotsset{compat=1.9} % avoid warnings

\input{../mymacros} % for convenience and consistency
\input{../fonts} % choose fonts from main thesis
\input{../units} % choose units from main thesis

\pgfplotsset{%
  proc/.style={%
    black,
    line width=1.1pt,
    arrows=-latex
  },
  refr/.style={%
    gray
  },
  range frame/.style={%
    tick align=outside,
    axis line style={opacity=0},
    after end axis/.code={
      \draw ({rel axis cs:0,0}-|{axis cs:\pgfplotsdataxmin,0}) -- ({rel axis cs:0,0}-|{axis cs:\pgfplotsdataxmax,0});
      \draw ({rel axis cs:0,0}|-{axis cs:0,\pgfplotsdataymin}) -- ({rel axis cs:0,0}|-{axis cs:0,\pgfplotsdataymax});
    }
  }
}

\tikzset{%
  stat/.style={%
    circle,
    draw,
    inner sep=1pt,
    outer sep=5pt
  }
}

\newcommand{\drawstate}[3]{%
  \draw[fill=white] (axis cs:#1) circle (2pt) node[stat,#2] {#3};
}

